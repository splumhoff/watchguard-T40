WatchGuard patches to sources for procps-3.3.12 as of
Mon May  9 14:00:27 PDT 2022
The patches shown here have been applied to source .tar.gz 
files supplied with the WatchGuard Open Source Archive.

==========================================================================
--- procps-3.3.12/ltmain.sh.orig	2022-05-09 14:00:24.531694896 -0700
+++ procps-3.3.12/ltmain.sh	2022-05-09 14:00:24.699688083 -0700
@@ -8367,7 +8367,8 @@
 	      fi
 	    else
 	      # We cannot seem to hardcode it, guess we'll fake it.
-	      add_dir=-L$libdir
+          # WG: do not fake it, this introduces host pollution
+	      # add_dir=-L$libdir
 	      # Try looking first in the location we're being installed to.
 	      if test -n "$inst_prefix_dir"; then
 		case $libdir in
